package version

// Stamp matches TEMPLATE_STAMP in the CT vztmpl filename (yyyymmdd_hhmmss).
var Stamp = "dev"

// TemplateBasename is the prefix used in exported CT template filenames.
const TemplateBasename = "pve-dns-lockdown_ct-template"

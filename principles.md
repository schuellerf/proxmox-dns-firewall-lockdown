# Principles

- **Humans and LLMs are treated the same** as readers: write for clarity once, usable by both without separate “modes.”
- **Documentation is minimal and crisp.** Say enough that someone understands the idea and can act; avoid length for its own sake.
